<?php

/**
 * Implements hook_theme().
 */
function theater_theme($existing, $type, $theme, $path) {
    return array(
        'terms_conditions' => array(
            'variables' => array('title' => '', 'terms' => '')
        )
    );
}